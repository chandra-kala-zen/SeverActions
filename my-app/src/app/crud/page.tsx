import Crudserver from "@/Components/crudserver/crudserver";

const page = async() => {
    return (
      <>
        <Crudserver params={undefined} />
      </>
    );
  };
  
  export default page;
